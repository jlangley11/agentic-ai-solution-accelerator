"""Scenario: contoso-supplier-risk.

Exports are intentionally empty — the scenario is consumed via
:mod:`src.workflow.registry` which resolves ``schema:`` and
``workflow_factory:`` by module path from ``accelerator.yaml``.
"""
